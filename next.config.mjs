/** @type {import('next').NextConfig} */

const nextConfig = {
  /* config options here */
  images: {
    domains: [
      'picsum.photos',
      'loremflickr.com',
      'localhost',
      'spotify-api-0gmj.onrender.com',
    ],
  },
};

export default nextConfig;
